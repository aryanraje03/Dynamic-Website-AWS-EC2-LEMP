name: AWS LEMP CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Display Success Message
        run: echo "GitHub Actions Pipeline Running Successfully"
